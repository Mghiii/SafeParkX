# SafeParkX

Application de gestion de parking avec Flask, caméra et temps réel (Socket.IO).

## Installation

```bash
python -m venv env
source env/bin/activate  # ou env\Scripts\activate sur Windows
pip install -r requirements.txt
```

## Lancer l'app

```bash
python main.py
```
